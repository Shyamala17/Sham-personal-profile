# 📰 Web Scraper for News Headlines

## 📌 Objective
Automate the collection of top headlines from a news website (or your own HTML file) using Python. The headlines are extracted and saved into a `.txt` file for easy access.

---

## 🛠️ Tools & Libraries
- **Python 3.x (64-bit)**
- **requests** → Fetch HTML content from websites
- **BeautifulSoup (bs4)** → Parse and extract data from HTML

---

## 📂 Deliverables
- `scraper.py` → Python script for scraping headlines
- `sample.html` → Example HTML file (for practice/demo)
- `headlines.txt` → Output file containing extracted headlines

---

## ⚙️ Setup Instructions
1. Install dependencies:
   ```bash
   pip install requests beautifulsoup4
